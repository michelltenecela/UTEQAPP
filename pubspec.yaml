name: uteq_news_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.4.3+1 # Para peticiones HTTP
  provider: ^6.1.2 # Para gestión de estado
  lottie: ^3.1.0 # Para animaciones Lottie
  font_awesome_flutter: ^10.7.0 # Para iconos Font Awesome
  material_design_icons_flutter: ^7.0.7296 # Para Material Community Icons
  shared_preferences: ^2.2.3 # Para almacenamiento local
  url_launcher: ^6.2.6 # Para abrir URLs

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/lottie/